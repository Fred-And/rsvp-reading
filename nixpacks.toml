[phases.setup]
# python3 + gcc + make needed to compile better-sqlite3 native addon
nixPkgs = ["nodejs_22", "python3", "gcc", "gnumake"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build", "npm prune --omit=dev"]

[start]
cmd = "node build"
